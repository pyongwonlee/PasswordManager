@model PasswordManager.Models.Entities.Password

@{
    ViewBag.Title = "Edit";
    ViewBag.SumbitTitle = "Save";
}

<h2>Edit Account</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    @Html.Partial("_PasswordForm", @Model)
}

<div>
    @Html.ActionLink("Back to List", "Index", new { categoryId = 0, page = 1 }, new { @class = "btn btn-link" })
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
